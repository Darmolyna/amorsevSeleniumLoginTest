name: Java CI with Maven

on:
  push:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven

    - name: Build with Maven
      run: mvn clean test
      working-directory: ./amorsevTest

    # Optional: Uploads the full dependency graph to GitHub to improve the quality of Dependabot alerts this repository can receive
    - name: Update dependency graph
      uses: advanced-security/maven-dependency-submission-action@v2
      with:
        working-directory: ./amorsevTest  # Set the working directory to where your pom.xml is located

    # Optional: Upload TestNG test results
    #- name: Archive test results
      #uses: actions/upload-artifact@v3
      #with:
        #name: test-results
        #path: amorsevTest/target/surefire-reports  # Adjust the path if needed
